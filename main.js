// Example JavaScript code to handle form submission or AJAX
document.getElementById("login-form").addEventListener("submit", function(event) {
    event.preventDefault();
    // You can add AJAX requests or client-side validation here
});
